name: HAC Forecast Automation

on:
  schedule:
    # Executa automaticamente a cada 6 horas
    - cron: "0 */6 * * *"
  workflow_dispatch:  # permite rodar manualmente
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  run-hac:
    runs-on: ubuntu-latest
    name: Executar previsão HAC automática

    steps:
      - name: 📥 Clonar repositório
        uses: actions/checkout@v3

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Adição para o novo fetcher (não afeta o antigo)
          pip install cdasws  # Para CDAWeb real-only

      - name: 🌞 Coletar dados solares (data_fetcher.py)  # Passo original mantido
        run: |
          python src/data_fetcher.py

      - name: 🔮 Rodar previsão heliogeofísica (heliopredictive.py)  # Passo original mantido
        run: |
          python src/heliopredictive.py

      - name: 💾 Salvar resultados e gráficos  # Passo original mantido
        run: |
          mkdir -p results
          mv *.png results/ || true

      - name: 🌞 Coletar dados solares REAL-ONLY (novo: data_fetcher_real_only.py)  # NOVO STEP - Não modifica nada antigo
        run: |
          python src/data_fetcher_real_only.py || echo "Falha no real-only - Continuando com dados padrão"
          # Salva em subpasta pra não conflitar
          mkdir -p data/real_only results/real_only plots/real_only
          mv data/solar_data_real_only_*.csv data/real_only/ || true
          mv data/collection_metadata.json data/real_only/ || true

      - name: 🧪 Rodar validação comparativa com dados real-only (novo: hac_validation_real_only.py)  # Opcional: adapte se tiver validador
        run: |
          # Se você tiver um hac_validation adaptado pro real-only, rode aqui
          # Exemplo: python src/hac_validation.py --use-real-only  # Ou crie um flag
          echo "Validação real-only executada (adapte conforme necessário)"

      - name: 📤 Commit e Push dos Resultados  # Passo original mantido, agora inclui novos arquivos
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add results/*.png || true
          git add data/real_only/* || true  # Adiciona novos sem tocar antigos
          git commit -m "Atualização automática de previsões HAC 🌍 + Real-Only Dados" || echo "Nada a atualizar"
          git push "https://github-actions[bot]:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" HEAD:main

      - name: 💾 Salvar relatório de validação  # Passo original mantido
        run: |
          mkdir -p results
          mv results/*validation* results/ || true
          git add results/*validation* || true
          git commit -m "Validação automática HAC x NOAA 🌞" || echo "Nada a validar"
          git push

      - name: 📦 Upload Artifact com Real-Only  # NOVO: Separa artifacts pra clareza
        uses: actions/upload-artifact@v4
        with:
          name: hac-real-only-results
          path: |
            data/real_only/
            results/real_only/
            plots/real_only/
